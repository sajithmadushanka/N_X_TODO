// prisma client 
// lib/prisma.ts
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();
export default prisma;
// This is the Prisma client that we will use to interact with the database. We will import this client in our API routes to interact with the database.
//



